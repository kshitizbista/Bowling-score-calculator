import { FrameFormatPipe } from './frame-format.pipe';

describe('FrameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FrameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
